export interface Theme {
    fonts: {
      body: string;
      heading: string;
    };
    fontWeights: {
      regular: number;
      medium: number;
      semibold:number;
      bold: number;
    };
  }
